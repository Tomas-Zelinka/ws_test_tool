<?xml version="1.0" encoding="UTF-8"?>

<project name="FIWS" default="default" basedir=".">
    <description>Builds, tests, and runs the project FIWS.</description>
    <property name="src.dir" value="src" />
   	<property name="build.dir" value="build" />
	<property name="lib.dir" value="lib" />
	
	<property name="class.dir" value="${build.dir}/classes" />
	<property name="jar.dir" value="${build.dir}/jar" />
	<property name="jar.file" value="${jar.dir}/${ant.project.name}.jar" />
	<property name="data.dir" value="${jar.dir}/dbdata" />
	<property name="resources.dir" value="${jar.dir}/dbdata" />
	
	<path id="classpath">
	  <fileset dir="${lib.dir}">
	    <include name="*.jar"/>
	  </fileset>
	  
	</path>

	<target name="clean">
		<delete dir="build" />
	</target>
	
	<target name="init" depends="clean">
		<mkdir dir="${build.dir}"/>
		<mkdir dir="${class.dir}" />
		<mkdir dir="${jar.dir}" />
		<mkdir dir="${data.dir}" />
		
		<copy todir="${data.dir}">
			<fileset dir="dbdata"/>
		</copy>  
    </target>
	
	<target name="compile" depends="clean,init" description="compile the source"  >
	    <echo>Compile from ${src.dir} to ${build.dir}</echo>
	
	    <javac destdir="${class.dir}" srcdir="${src.dir}" includeantruntime="false">
	    	<classpath refid="classpath"/>
	    </javac>
    </target>
	
	<target name="jar" depends="compile">
		<jar destfile="${jar.file}" basedir="${class.dir}">
			<manifest>
		    	<attribute name="Main-Class" value="cz.vutbr.fit.dip.fiws.presentation.MainWindow"/>
		    </manifest>
		</jar>
	</target>
	
	
	<target name="run" >
		<echo> Running ${jar.file} </echo>
		<java jar="${jar.file}" fork="true" />
	</target>
	
	
	
	
	<target name="default" depends="jar" />
	 
	
</project>
